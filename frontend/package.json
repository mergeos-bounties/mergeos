{
  "name": "mergeos-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run local",
    "local": "node server.js --mode local",
    "production": "node server.js --mode production --prod",
    "build": "npm run build:production",
    "build:local": "vite build --mode development --outDir dist/client --ssrManifest && vite build --mode development --ssr src/entry-server.js --outDir dist/server",
    "build:production": "vite build --mode production --outDir dist/client --ssrManifest && vite build --mode production --ssr src/entry-server.js --outDir dist/server",
    "preview": "npm run production",
    "test": "node --test server.test.js"
  },
  "dependencies": {
    "@lucide/vue": "latest",
    "@vue/server-renderer": "latest",
    "vue": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.0.16"
  }
}
